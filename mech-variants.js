var mechs =
{
	"JR7":
	{
		maxTonnage:35,
		variants:
		{
			  "D":{minEngine:100, maxEngine:300}
			, "F":{minEngine:100, maxEngine:300}
			, "K":{minEngine:100, maxEngine:300}
			, "F(C)":{minEngine:100, maxEngine:300}
			, "D(S)":{minEngine:100, maxEngine:300}
		}
	}

	, "AS7":
	{
		maxTonnage:100,
		variants:
		{
			  "D":{minEngine:200, maxEngine:360}
			, "K":{minEngine:200, maxEngine:360}
			, "RS":{minEngine:200, maxEngine:360}
			, "D-DC":{minEngine:200, maxEngine:360}
			, "RS(C)":{minEngine:200, maxEngine:360}
		}
	}
};

